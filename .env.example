# Embeddings
EMBEDDING_PROVIDER=hashing
EMBEDDING_MODEL=

# LLM
LLM_PROVIDER=rule_based
LLM_MODEL=

# AWS (if using bedrock providers)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# Long-term memory persistence
LTM_PERSIST_DIR=./data/ltm

# Demo interactions (0 disables synthetic demo to keep real memory clean)
DEMO_INTERACTIONS=0
